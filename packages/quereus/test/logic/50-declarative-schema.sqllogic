-- Minimal declarative schema lifecycle smoke test

declare schema main {
  table users (
    id integer primary key,
    name text
  );

  seed users values (id, name) values (1, 'Alice'), (2, 'Bob');
}

-- run

diff schema main;
→ [{"status":"[]"}]

apply schema main;
→ [{"status":"ok"}]

explain schema main;
→ [{"status":"hash:0"}]

