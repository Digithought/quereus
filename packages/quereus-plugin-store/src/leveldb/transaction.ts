/**
 * Transaction coordinator for LevelDB module.
 *
 * Re-exports the common TransactionCoordinator for backwards compatibility.
 */

export { TransactionCoordinator, type TransactionCallbacks } from '../common/transaction.js';

