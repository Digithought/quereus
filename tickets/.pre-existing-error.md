## Pre-existing test failure: quereus-isolation type-check

**Surfaced during review of**: `fix-ast-stringify-check-ops-and-compound-select`.

**Command that surfaces it** (from repo root):

```
yarn test
```

It halts at `packages/quereus-isolation` with a TypeScript exhaustive-check failure
during build/typecheck — the run never reaches the actual mocha suite for that
package. The quereus package's own test run (`yarn workspace @quereus/quereus
run test`) is clean: 3226 passing, 0 failures, both with and without this
ticket's changes.

**Location**: `packages/quereus-isolation/src/isolation-module.ts:564`

```
default: {
    const _exhaustive: never = change;
    newData = data;
}
```

The `case` arms above handle `dropColumn`, `renameColumn`, `alterColumn`, and
`alterPrimaryKey`. The `change` discriminated-union type must now include a
variant (likely `addConstraint`) that is not in the switch, so TS rejects the
assignment to `never`.

**Why pre-existing**: The implement-stage handoff for
`fix-ast-stringify-check-ops-and-compound-select` already documented this
("`yarn test` at the repo root halts at `packages/quereus-isolation` with a
TypeScript exhaustive-check error … Confirmed pre-existing on main via stash +
replay — unrelated to this ticket"). The fix only touches
`packages/quereus/src/emit/ast-stringify.ts` and tests in `packages/quereus/`;
it has no connection to the isolation module's `change` union type or to ALTER
TABLE statement variants.

**Work-around used during this ticket**: validated quereus package directly via
`yarn workspace @quereus/quereus run test` (clean, 3226 passing) rather than the
root `yarn test`. No source changes were made to suppress or skip the isolation
failure.
